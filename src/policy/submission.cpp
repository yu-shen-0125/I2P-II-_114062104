/*
 * Mini Project 2 submission policy.
 *
 * The implementation is kept in minimax.cpp for this workspace because the
 * supplied Makefile compiles all C++ files under src/policy with a wildcard.
 * Including minimax.cpp here would duplicate all policy symbols and break the
 * required unchanged Makefile build.
 */
